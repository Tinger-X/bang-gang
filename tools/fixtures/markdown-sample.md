# 渲染要素总览

这是一段**正文**，里面有 `行内代码`、*斜体*、**粗体**、***粗斜***、~~删除线~~，
还有一个[链接文字](https://example.com/page)和一处转义的 \* 星号。

## 二级标题

中文段落在这里换行折行测试：帮帮是一个直播助手，主窗口对所有截屏软件完全不可见，
只有本机用户看得见，界面里是聊天。

### 三级标题

- 无序列表第一项
- 无序列表第二项，这一项比较长，长到必须在气泡里折行才能放得下，用来看看折行之后的
  第二行有没有和第一项的文字对齐，而不是跑到项目符号下面去
  - 嵌套的无序项
  - 另一个嵌套项
- 无序列表第三项

1. 有序列表第一项
2. 有序列表第二项
3. 有序列表第三项

> 引用块的第一行。
> 引用的第二行。
>
> > 嵌套的引用。

```csharp
// 一段 C# 代码，用来看高亮和左上角的语言标签
public static int Add(int a, int b)
{
    string s = "hello, 世界";
    return a + b;   // 36
}
```

```python
def greet(name):
    """打招呼"""
    print(f"hi {name}")
    return None
```

| 名称 | 类型 | 说明 |
|:-----|:----:|-----:|
| alpha | int | 左对齐的一列 |
| beta | string | 中间这一列 |
| gamma | bool | 右边这一列 |

---

结尾的一段普通文字，用来确认分隔线下面还有内容。
